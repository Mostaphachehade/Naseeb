PORT=3000
JWT_SECRET=change-this-to-a-long-random-string
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Public URL this app is served at — used to build links in emails
# (verification, password reset). Use http://localhost:3000 for local dev.
APP_URL=http://localhost:3000

# Optional: without these, emails are logged to the console instead of sent.
RESEND_API_KEY=
EMAIL_FROM=Naseeb <onboarding@resend.dev>
# Where "new host application" notifications are sent.
ADMIN_NOTIFY_EMAIL=

# Optional: without these, image upload is hidden and hosts paste a URL instead.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_UPLOAD_PRESET=
